export const metadata = {
  title: "Admin – Portfolio CMS",
  description: "Portfolio admin management panel",
  robots: "noindex, nofollow",
};

export default function AdminLayout({ children }) {
  return <>{children}</>;
}
